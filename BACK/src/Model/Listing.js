class Listing {
	constructor(title, text, like_number, id_user) {
		this.title = title;
		this.text = text;
		this.like_number = like_number;
		this.id_user = id_user;
	}
}

module.exports = { Listing };
